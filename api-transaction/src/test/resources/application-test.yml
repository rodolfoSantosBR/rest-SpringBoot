server:
  port: 8888
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: false
#  flyway:
#    default-schema: rest_with_spring_boot_erudio


#  h2:
#    console.enabled: true
#
#  flyway:
#   schemas: rest_with_spring_boot_erudio

springdoc:
  api-docs:
    path: /microservicos/api-docs
  swagger-ui:
    use-root-path: true
